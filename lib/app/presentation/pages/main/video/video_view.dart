import 'package:flutter/widgets.dart';

class VideoView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('VideoView'),
      ),
    );
  }
}
